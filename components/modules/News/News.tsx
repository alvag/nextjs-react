import React from 'react';
import { INews } from '../../../Interfaces';

interface IProps {
    news: INews[];
}

const News = (props: IProps) => {
    return (
        <div>

        </div>
    );
};

export default News;
